<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Track free models on OpenRouter, ranked by coding capability">
  <title>OpenRouter Free Models Tracker</title>
  <link rel="alternate" type="application/atom+xml" title="OpenRouter Free Models Feed" href="/atom.xml">
  <style>
    :root {
      --bg: #ffffff;
      --fg: #1d1d1f;
      --muted: #86868b;
      --border: #d2d2d7;
      --accent: #0071e3;
      --accent-hover: #0077ed;
      --green: #30d158;
      --yellow: #ffd60a;
      --red: #ff453a;
      --card: #f5f5f7;
      --code-bg: #f0f0f0;
      --header-bg: #f5f5f7;
      --row-hover: #f0f0f0;
      --shadow: rgba(0, 0, 0, 0.08);
      --toast-bg: #1d1d1f;
      --toast-fg: #ffffff;
    }

    @media (prefers-color-scheme: dark) {
      :root {
        --bg: #1d1d1f;
        --fg: #f5f5f7;
        --muted: #8e8e93;
        --border: #3a3a3c;
        --accent: #0a84ff;
        --accent-hover: #409cff;
        --green: #32d74b;
        --yellow: #ffd60a;
        --red: #ff453a;
        --card: #2c2c2e;
        --code-bg: #2c2c2e;
        --header-bg: #2c2c2e;
        --row-hover: #3a3a3c;
        --shadow: rgba(0, 0, 0, 0.3);
        --toast-bg: #f5f5f7;
        --toast-fg: #1d1d1f;
      }
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      font-size: 16px;
    }

    body {
      font-family: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      background: var(--bg);
      color: var(--fg);
      line-height: 1.5;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* Layout */
    .container {
      width: 100%;
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 1rem;
      flex: 1;
    }

    header {
      padding: 2rem 1rem;
      border-bottom: 1px solid var(--border);
      background: var(--header-bg);
    }

    .header-content {
      max-width: 1400px;
      margin: 0 auto;
    }

    h1 {
      font-size: clamp(1.5rem, 4vw, 2.5rem);
      font-weight: 600;
      letter-spacing: -0.02em;
      margin-bottom: 0.5rem;
    }

    .subtitle {
      color: var(--muted);
      font-size: clamp(0.875rem, 2vw, 1rem);
    }

    .meta {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      margin-top: 1rem;
      font-size: 0.875rem;
      color: var(--muted);
    }

    .meta a {
      color: var(--accent);
      text-decoration: none;
    }

    .meta a:hover {
      text-decoration: underline;
    }

    main {
      flex: 1;
      padding: 1.5rem 1rem;
    }

    /* Table */
    .table-wrapper {
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
      border: 1px solid var(--border);
      border-radius: 12px;
      background: var(--card);
      box-shadow: 0 4px 20px var(--shadow);
    }

    table {
      width: 100%;
      border-collapse: collapse;
      min-width: 800px;
    }

    thead {
      position: sticky;
      top: 0;
      z-index: 10;
      background: var(--header-bg);
    }

    th {
      padding: 0.875rem 1rem;
      text-align: left;
      font-weight: 600;
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--muted);
      border-bottom: 2px solid var(--border);
      white-space: nowrap;
      cursor: pointer;
      user-select: none;
      transition: color 0.15s ease;
    }

    th:hover {
      color: var(--fg);
    }

    th.sorted-asc::after { content: " ▲"; }
    th.sorted-desc::after { content: " ▼"; }

    td {
      padding: 0.875rem 1rem;
      border-bottom: 1px solid var(--border);
      font-size: 0.875rem;
      vertical-align: middle;
    }

    tbody tr {
      transition: background 0.15s ease;
    }

    tbody tr:hover {
      background: var(--row-hover);
    }

    tbody tr:last-child td {
      border-bottom: none;
    }

    /* Column widths */
    .col-rank { width: 48px; }
    .col-name { width: 280px; min-width: 200px; }
    .col-coding { width: 100px; }
    .col-intel { width: 110px; }
    .col-context { width: 110px; }
    .col-modalities { width: 160px; }
    .col-released { width: 110px; }
    .col-reasoning { width: 180px; }
    .col-desc { min-width: 280px; }
    .col-copy { width: 48px; }

    /* Cells */
    .rank {
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
      font-weight: 600;
      color: var(--muted);
    }

    .model-name {
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
    }

    .model-name .name {
      font-weight: 600;
      font-size: 0.875rem;
    }

    .model-name .id {
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
      font-size: 0.7rem;
      color: var(--muted);
    }

    .model-name .expired-badge {
      display: inline-block;
      font-size: 0.625rem;
      font-weight: 600;
      padding: 0.125rem 0.375rem;
      border-radius: 3px;
      background: var(--red);
      color: white;
      text-transform: uppercase;
      letter-spacing: 0.02em;
    }

    .coding-index {
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
      font-weight: 600;
      font-size: 1rem;
    }

    .coding-index.high { color: var(--green); }
    .coding-index.medium { color: var(--yellow); }
    .coding-index.low { color: var(--red); }
    .coding-index.na { color: var(--muted); }

    .intelligence-index {
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
      font-size: 0.875rem;
      color: var(--muted);
    }

    .context-length {
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
      font-size: 0.875rem;
    }

    .modalities {
      display: flex;
      flex-wrap: wrap;
      gap: 0.375rem;
    }

    .modality-badge {
      font-size: 0.6875rem;
      padding: 0.1875rem 0.5rem;
      border-radius: 4px;
      background: var(--code-bg);
      border: 1px solid var(--border);
      white-space: nowrap;
    }

    .release-date {
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
      font-size: 0.8125rem;
      color: var(--muted);
    }

    .reasoning {
      font-size: 0.75rem;
      color: var(--muted);
      max-width: 180px;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .description {
      font-size: 0.8125rem;
      color: var(--fg);
      max-width: 400px;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }

    .copy-btn {
      background: none;
      border: none;
      cursor: pointer;
      padding: 0.375rem;
      border-radius: 6px;
      color: var(--muted);
      transition: all 0.15s ease;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .copy-btn:hover {
      background: var(--code-bg);
      color: var(--fg);
    }

    .copy-btn.copied {
      color: var(--green);
    }

    .copy-btn svg {
      width: 18px;
      height: 18px;
    }

    /* Expired row */
    tr.expired {
      opacity: 0.6;
    }

    tr.expired .model-name .name {
      text-decoration: line-through;
    }

    /* Footer */
    footer {
      padding: 2rem 1rem;
      border-top: 1px solid var(--border);
      background: var(--header-bg);
    }

    .footer-content {
      max-width: 1400px;
      margin: 0 auto;
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      align-items: center;
      gap: 1rem;
      font-size: 0.8125rem;
      color: var(--muted);
    }

    .footer-links {
      display: flex;
      gap: 1.5rem;
    }

    .footer-links a {
      color: var(--accent);
      text-decoration: none;
    }

    .footer-links a:hover {
      text-decoration: underline;
    }

    /* Toast */
    .toast {
      position: fixed;
      bottom: 1.5rem;
      left: 50%;
      transform: translateX(-50%) translateY(100px);
      opacity: 0;
      background: var(--toast-bg);
      color: var(--toast-fg);
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      font-size: 0.875rem;
      font-weight: 500;
      box-shadow: 0 8px 30px var(--shadow);
      z-index: 1000;
      transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .toast.show {
      transform: translateX(-50%) translateY(0);
      opacity: 1;
    }

    /* Responsive */
    @media (max-width: 1024px) {
      .col-intel, .col-reasoning {
        display: none;
      }
      
      th.col-intel, th.col-reasoning {
        display: none;
      }
    }

    @media (max-width: 640px) {
      .col-modalities, .col-released {
        display: none;
      }
      
      th.col-modalities, th.col-released {
        display: none;
      }
      
      .description {
        max-width: 200px;
      }
      
      header {
        padding: 1.5rem 1rem;
      }
      
      main {
        padding: 1rem;
      }
      
      th, td {
        padding: 0.625rem 0.75rem;
      }
    }

    /* Reduced motion */
    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
      }
    }

    /* Focus visible */
    :focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
    }
  </style>
</head>
<body>
  <header>
    <div class="header-content">
      <h1>OpenRouter Free Models</h1>
      <p class="subtitle">Free models ranked by coding capability (Artificial Analysis Coding Index)</p>
      <div class="meta">
        <span>{{ total_count }} models</span>
        <span>Updated: {{ last_updated }}</span>
        <a href="/atom.xml">📡 Atom Feed</a>
        <a href="https://github.com/{{ github_username }}/{{ repo_name }}" target="_blank" rel="noopener">Source</a>
        <a href="https://openrouter.ai/models" target="_blank" rel="noopener">OpenRouter</a>
      </div>
    </div>
  </header>

  <main>
    <div class="table-wrapper" role="region" aria-label="Free models table" tabindex="0">
      <table id="models-table">
        <thead>
          <tr>
            <th class="col-rank" data-sort="rank">#</th>
            <th class="col-name" data-sort="name">Model</th>
            <th class="col-coding" data-sort="coding">Coding</th>
            <th class="col-intel" data-sort="intelligence">Intelligence</th>
            <th class="col-context" data-sort="context">Context</th>
            <th class="col-modalities" data-sort="modalities">Modalities</th>
            <th class="col-released" data-sort="released">Released</th>
            <th class="col-reasoning" data-sort="reasoning">Reasoning</th>
            <th class="col-desc">Description</th>
            <th class="col-copy" aria-label="Copy model ID"></th>
          </tr>
        </thead>
        <tbody>
          {% for model in models %}
          <tr class="{% if model.is_expired %}expired{% endif %}" data-rank="{{ model.rank }}" data-name="{{ model.name }}" data-coding="{{ model.coding_index or -1 }}" data-intelligence="{{ model.intelligence_index or -1 }}" data-context="{{ model.context_length }}" data-modalities="{{ model.modalities_badges | join(',') }}" data-released="{{ model.release_date.timestamp() }}" data-reasoning="{{ model.reasoning.default_effort if model.reasoning else '' }}">
            <td class="rank">{{ model.rank }}</td>
            <td class="col-name">
              <div class="model-name">
                <span class="name">{{ model.name }}</span>
                <span class="id">{{ model.id }}</span>
                {% if model.is_expired %}
                <span class="expired-badge">Expired</span>
                {% endif %}
              </div>
            </td>
            <td class="col-coding">
              <span class="coding-index {{ coding_index_color(model.coding_index) }}">
                {% if model.coding_index is not none %}{{ "%.1f"|format(model.coding_index) }}{% else %}N/A{% endif %}
              </span>
            </td>
            <td class="col-intel">
              <span class="intelligence-index">
                {% if model.intelligence_index is not none %}{{ "%.1f"|format(model.intelligence_index) }}{% else %}N/A{% endif %}
              </span>
            </td>
            <td class="col-context">
              <span class="context-length">{{ model.context_length | humanize_tokens }}</span>
            </td>
            <td class="col-modalities">
              <div class="modalities">
                {% for badge in model.modalities_badges %}
                <span class="modality-badge">{{ badge }}</span>
                {% endfor %}
              </div>
            </td>
            <td class="col-released">
              <time class="release-date" datetime="{{ model.release_date.isoformat() }}">
                {{ model.release_date.strftime('%Y-%m-%d') }}
              </time>
            </td>
            <td class="col-reasoning">
              <span class="reasoning" title="{{ model.reasoning.supported_efforts | join(', ') if model.reasoning else 'N/A' }}">
                {{ model.reasoning | reasoning_summary }}
              </span>
            </td>
            <td class="col-desc">
              <span class="description" title="{{ model.description }}">{{ model.description }}</span>
            </td>
            <td class="col-copy">
              <button class="copy-btn" data-model-id="{{ model.id }}" aria-label="Copy {{ model.id }}" title="Copy model ID">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path></svg>
              </button>
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </main>

  <footer>
    <div class="footer-content">
      <p>Data from <a href="https://openrouter.ai" target="_blank" rel="noopener">OpenRouter API</a> • Rankings by <a href="https://artificialanalysis.ai" target="_blank" rel="noopener">Artificial Analysis</a></p>
      <div class="footer-links">
        <a href="/atom.xml">Atom Feed</a>
        <a href="https://github.com/{{ github_username }}/{{ repo_name }}" target="_blank" rel="noopener">GitHub</a>
        <a href="https://openrouter.ai/models?max_price=0" target="_blank" rel="noopener">Browse on OpenRouter</a>
      </div>
    </div>
  </footer>

  <div class="toast" id="toast" role="status" aria-live="polite">Copied!</div>

  <script>
    (function() {
      'use strict';

      // Toast notification
      const toast = document.getElementById('toast');
      function showToast(message = 'Copied!') {
        toast.textContent = message;
        toast.classList.add('show');
        setTimeout(() => toast.classList.remove('show'), 2000);
      }

      // Copy button functionality
      document.querySelectorAll('.copy-btn').forEach(btn => {
        btn.addEventListener('click', async (e) => {
          e.stopPropagation();
          const modelId = btn.dataset.modelId;
          try {
            await navigator.clipboard.writeText(modelId);
            btn.classList.add('copied');
            showToast(`Copied ${modelId}`);
            setTimeout(() => btn.classList.remove('copied'), 2000);
          } catch (err) {
            showToast('Failed to copy');
            console.error('Copy failed:', err);
          }
        });
      });

      // Sortable table
      const table = document.getElementById('models-table');
      const tbody = table.querySelector('tbody');
      const headers = table.querySelectorAll('th[data-sort]');
      let sortState = {};

      function getCellValue(row, key) {
        return row.dataset[key] || '';
      }

      function compareRows(a, b, key, direction) {
        const aVal = getCellValue(a, key);
        const bVal = getCellValue(b, key);
        
        // Numeric comparison for certain columns
        const numericKeys = ['rank', 'coding', 'intelligence', 'context', 'released'];
        if (numericKeys.includes(key)) {
          const aNum = parseFloat(aVal);
          const bNum = parseFloat(bVal);
          if (!isNaN(aNum) && !isNaN(bNum)) {
            return direction === 'asc' ? aNum - bNum : bNum - aNum;
          }
        }
        
        // String comparison
        return direction === 'asc' 
          ? aVal.localeCompare(bVal, undefined, { numeric: true })
          : bVal.localeCompare(aVal, undefined, { numeric: true });
      }

      function sortTable(key) {
        const currentDir = sortState[key] || 'asc';
        const newDir = currentDir === 'asc' ? 'desc' : 'asc';
        sortState[key] = newDir;

        // Reset other headers
        headers.forEach(h => {
          if (h.dataset.sort !== key) {
            h.classList.remove('sorted-asc', 'sorted-desc');
            delete sortState[h.dataset.sort];
          }
        });

        // Update current header
        const header = table.querySelector(`th[data-sort="${key}"]`);
        header.classList.remove('sorted-asc', 'sorted-desc');
        header.classList.add(newDir === 'asc' ? 'sorted-asc' : 'sorted-desc');

        // Sort rows
        const rows = Array.from(tbody.querySelectorAll('tr'));
        rows.sort((a, b) => compareRows(a, b, key, newDir));
        rows.forEach(row => tbody.appendChild(row));
      }

      headers.forEach(header => {
        header.addEventListener('click', () => {
          sortTable(header.dataset.sort);
        });
        
        header.addEventListener('keydown', (e) => {
          if (e.key === 'Enter' || e.key === ' ') {
            e.preventDefault();
            sortTable(header.dataset.sort);
          }
        });
      });

      // Default sort by rank (already sorted)
      const rankHeader = table.querySelector('th[data-sort="rank"]');
      rankHeader.classList.add('sorted-asc');
      sortState['rank'] = 'asc';

      // Keyboard navigation for table
      table.addEventListener('keydown', (e) => {
        if (e.key === 'Tab') return; // Let browser handle tab
        
        const focused = document.activeElement;
        if (!focused.closest('tr')) return;
        
        const row = focused.closest('tr');
        const rows = Array.from(tbody.querySelectorAll('tr'));
        const idx = rows.indexOf(row);
        
        let targetRow = null;
        if (e.key === 'ArrowDown' && idx < rows.length - 1) {
          targetRow = rows[idx + 1];
        } else if (e.key === 'ArrowUp' && idx > 0) {
          targetRow = rows[idx - 1];
        } else if (e.key === 'Home') {
          targetRow = rows[0];
        } else if (e.key === 'End') {
          targetRow = rows[rows.length - 1];
        }
        
        if (targetRow) {
          e.preventDefault();
          const targetCell = targetRow.querySelector('td:not(.col-copy)') || targetRow.querySelector('td');
          targetCell?.focus({ preventScroll: true });
        }
      });

      // Make rows focusable for keyboard nav
      tbody.querySelectorAll('tr').forEach(row => {
        row.setAttribute('tabindex', '0');
      });

    })();
  </script>
</body>
</html>